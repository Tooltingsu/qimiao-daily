# V4-B execution continuation prompt

Read V4_B_EXECUTION_PLAN.md and V4_GITHUB_RUNTIME_QA.md, then inspect Git status and live GitHub run state. Target is exclusively Tooltingsu/qimiao-daily. Preserve V3 and all five V4 data layers. Implement only migration/runtime fixes and missing V4-B requirements; do not redesign the proven core.

Continue DISCOVER → IMPLEMENT → RUN → OBSERVE → FIX → RE-RUN → REVIEW. Use hosted Linux evidence, never substitutes or fake provider successes. All publication is DRY_RUN, with null QQ IDs and no real published timestamp. Pixiv LOGIN_REQUIRED is allowed. Keep locks, report hash/source commit, selected artwork snapshots and append-only attempts traceable.

All writers share concurrency; stage only their allowed output paths, verify data/ unchanged, fetch/rebase/retry without force, fail on conflicts. Never expose credentials or use PR code with secrets. Deploy a static Pages projection with no backend. Record all run IDs and capture live screenshots. Do not claim V4-B PASS until every supplied acceptance gate is proven. Stop after V4-B and wait for acceptance before QQ integration.
